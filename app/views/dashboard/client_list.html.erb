	<p class="lead" style="margin-bottom:0px;">Client List</p>
	<table class="table table-striped table-condensed" id="project-list">
		<tr>
			<th>ID</th>
			<th>Name</th>
			<th>Projects</th>
		</tr>
	<% @clients.each do |client| %>

			<tr>
				<td><%= client.id %></td>
				<td><a data-target="#remote_modal" href="/client?id=<%=client.id%>" role="button" data-toggle="modal"><%= client.name %></a></td>
				<td>
					<% projects = [] %>
					<% client.projects.each do |pro| %>
						<% projects << pro.name %>
					<% end %>
					<%= projects.join(', ') %>
				</td>

	</tr>

	<% end %>
		</table>
	<hr style="margin:0"/>
	<div id="project_list">

	</div>
	<a data-target="#remote_modal" href="/client" role="button" data-toggle="modal" class="btn btn-primary">Invite New Client <i class="icon-plus icon-white"></i></a>
</div>

<!-- Modal -->
<div id="remote_modal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Client Management</h3>
  </div>
  <div class="modal-body">
  </div>
</div>